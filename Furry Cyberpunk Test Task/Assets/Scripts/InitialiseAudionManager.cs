using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitialiseAudionManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        AudionManger init = AudionManger.inter;
    }

}
